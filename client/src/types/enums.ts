export enum Message {
  PlayerSelection,
}

export enum Cell {
  Empty,
  X,
  O,
}

export enum TicTacResults {
  STILL_PLAYING = "STILL_PLAYING",
  YOU_WON = "YOU_WON",
  OPPONENT_WON = "OPPONENT_WON",
}
